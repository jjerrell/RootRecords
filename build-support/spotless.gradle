apply plugin:  "com.diffplug.spotless"
spotless {
    kotlin {
        ktfmt()
            .kotlinlangStyle()
        target("src/*/kotlin/**/*.kt")
        targetExclude("${layout.buildDirectory}/**/*.kt")
    }
}